#include 'protheus.ch'
#include 'parmtype.ch'

User Function VLDZ19() 

Local lRet := .T.
Local aArea := GetArea() 
Local cChave:=  AllTrim(M->Z19_CHVNFE)
	
	If Len(AllTrim(cChave)) = 44
		lRet := .F.
	Else
		lRet 
    EndIf

RestArea(aArea) 
Return (lRet)
